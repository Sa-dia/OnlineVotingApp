<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="20dp"
    android:id="@+id/main"
    android:background="@drawable/candi_back">

    <!-- Title -->
    <TextView
        android:id="@+id/identifyVerificationTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="User Verification"
        android:textSize="26sp"
        android:textColor="@android:color/black"
        android:textStyle="bold"
        android:gravity="center"
        android:paddingBottom="40dp" />

    <!-- User Image -->
    <ImageView
        android:id="@+id/userLoginImage"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginBottom="20dp"
        android:src="@drawable/login_icon"
        android:contentDescription="User Login Image"
        android:layout_gravity="center" />

    <!-- Buttons Row -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="20dp">

        <!-- Voter Login Button -->
        <Button
            android:id="@+id/voterLoginButton"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:text="Voter Login"
            android:textColor="@android:color/white"
            android:backgroundTint="@color/black"
            android:textSize="16sp"
            android:textStyle="bold"
            android:gravity="center"
            android:padding="5dp"
            android:onClick="goToVoterLogin"
            android:layout_margin="10dp" />

        <!-- Admin Login Button -->
        <Button
            android:id="@+id/adminLoginButton"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:text="Admin Login"
            android:textColor="@android:color/white"
            android:backgroundTint="@color/darker_gray"
            android:textSize="16sp"
            android:textStyle="bold"
            android:gravity="center"
            android:padding="5dp"
            android:onClick="goToAdminLogin"
            android:layout_margin="10dp" />
    </LinearLayout>

</LinearLayout>
